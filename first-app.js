const fs = require('fs');  //Así se importan módulos en JS
//Así se escriben las constantes en JS (no me gusta, la verdad)

fs.writeFileSync('hello.txt', 'Hello from Node.js'); //Esta es una manera extraña de JS de crear archivos con contenido (viva Python)g